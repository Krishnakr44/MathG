/**
 * MathG — Geometry Engine Types
 * Re-export and extend core geometry types
 */

export type {
  GeometryViewMode,
  ShapeDimensions,
  NetFace,
  NetDefinition,
  CoverageUnit,
  IGeometryShape,
  GeometryParameterDef,
} from '@/core/types/geometry.types';
